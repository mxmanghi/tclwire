# Legacy Implementation

This directory contains the archived pre-`tcl/` TclWire implementation.

The files are retained for historical reference only. New development,
maintenance, and fixes belong in the packages under `tcl/`. Do not make
functional changes to the code in this directory.

The archived implementation preserves its original source relationships and
is not part of the current package index or test suite.
